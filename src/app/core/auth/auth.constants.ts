import { environment } from '../../../environments/environment';

export const AUTH_LOGIN_ENDPOINT = `${environment.apiUrl}/api/auth/login`;
export const API_ENDPOINT_PREFIX = `${environment.apiUrl}/api/`;
